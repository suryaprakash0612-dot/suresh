from selenium import webdriver
import time

driver = webdriver.Edge()
driver.get("https://www.facebook.com")

print("Facebook opened")
time.sleep(10)

driver.quit()

google:

from selenium import webdriver
from selenium.webdriver.common.by import By
import time

driver = webdriver.Edge()
driver.get("https://www.google.com")

print(driver.find_element(By.NAME, "q").is_displayed())

print("TEST passed")
time.sleep(15)

driver.quit()

  
  google:
from selenium import webdriver
from selenium.webdriver.common.by import By
import time

driver = webdriver.Edge()
driver.get("https://www.google.com")

driver.find_element(By.NAME, "q")

print("TEST passed")
time.sleep(15)

driver.quit()
  
git add .
git remote add origin "
git branch -m master
git push -u origin master

git checkout -b "MITS"
git branch
git merge MITS
git checkout master
git merge MITS
git branch



Running MySQL in data base and executing queries


docker --version
docker pull mysql:latest
docker run --name mysql-container -e MYSQL_ROOT_PASSWORD=root123 -d -p 3306:3306 mysql:latest
docker ps
docker exec -it mysql-container mysql -u root -p
root123
SHOW DATABASES;
CREATE DATABASE testdb;
USE testdb;
CREATE TABLE Students (
   id INT,
   name VARCHAR(50)
);
INSERT INTO Students VALUES (1,'John');
SELECT * FROM Students;





BASIC DOCKER COMMANDS
docker --version
docker pull ubuntu
docker images
docker run -it ubuntu
exit
docker ps
docker ps -a
docker start <container_id>
docker stop <container_id>
docker rm <container_id>
docker rmi ubuntu
cd "copy_path"
rename dockerfile.txt Dockerfile
docker build -t mycustomimage .
docker run mycustomimage


Code in note pad

FROM ubuntu
RUN apt-get update && apt-get install -y curl
CMD ["echo", "Hello, Docker!"]
